jobs:
  - name: rotate-mysql-password-adg_writer-development
    plan:
      - .: (( inject meta.plan.rotate-adg_writer-password ))
        config:
          params:
            AWS_ACC: ((aws_account.development))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.development)):role/ci

  - name: rotate-mysql-password-adg_writer-qa
    plan:
      - .: (( inject meta.plan.rotate-adg_writer-password ))
        config:
          params:
            AWS_ACC: ((aws_account.qa))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.qa)):role/ci

  - name: rotate-mysql-password-adg_writer-integration
    plan:
      - .: (( inject meta.plan.rotate-adg_writer-password ))
        config:
          params:
            AWS_ACC: ((aws_account.integration))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.integration)):role/ci

  - name: rotate-mysql-password-adg_writer-preprod
    plan:
      - .: (( inject meta.plan.rotate-adg_writer-password ))
        config:
          params:
            AWS_ACC: ((aws_account.preprod))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.preprod)):role/ci

  - name: rotate-mysql-password-adg_writer-production
    plan:
      - .: (( inject meta.plan.rotate-adg_writer-password ))
        config:
          params:
            AWS_ACC: ((aws_account.production))
            AWS_ROLE_ARN: arn:aws:iam::((aws_account.production)):role/ci
