groups:
  - name: master
    jobs:
      - analytical-dataset-generation-dev
      - analytical-dataset-generation-qa
      - analytical-dataset-generation-integration
      - analytical-dataset-generation-preprod
      - analytical-dataset-generation-production
  - name: pull-request
    jobs:
      - analytical-dataset-generation-pr
  - name: dataload
    jobs:
      - synthetic-data-ingestion-dev
      - synthetic-data-ingestion-integration
      - synthetic-data-ingestion-qa
  - name: admin-dev
    jobs:
      - dev-start-cluster
      - dev-stop-cluster
  - name: admin-qa
    jobs:
      - qa-start-cluster
      - qa-stop-cluster
  - name: admin-int
    jobs:
      - int-start-cluster
      - int-stop-cluster
  - name: admin-preprod
    jobs:
      - preprod-start-cluster
      - preprod-stop-cluster
  - name: admin-prod
    jobs:
      - prod-start-cluster
      - prod-stop-cluster
  - name: rotate-passwords-datareader
    jobs:
      - rotate-mysql-password-datareader-development
      - rotate-mysql-password-datareader-qa
      - rotate-mysql-password-datareader-integration
      - rotate-mysql-password-datareader-preprod
      - rotate-mysql-password-datareader-production